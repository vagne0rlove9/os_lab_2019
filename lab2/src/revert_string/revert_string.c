#include "revert_string.h"
#include "string.h"
#include <stdio.h>
void RevertString(char *str)
{
	char *new_str;
    int len = strlen(str) + 1;
    printf("%d asdada",len);
    // for(int i=0; i<strlen(str);i++, len--)
    // {
    //     new_str[i] = str[len];
    // }
    // printf("%s",str);
}

